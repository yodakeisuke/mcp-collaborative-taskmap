export const mergeToolDescription = `
As a developer, after you’ve merged the reviewed branch into the feature branch, update the status accordingly.
`;

export const nextAction = (): string => `
  If you haven’t already, merge your personal working branch from your worktree into the original worktree’s feature branch.
  Independently identify the next task, self-assign it, and proactively drive it to completion.
`;