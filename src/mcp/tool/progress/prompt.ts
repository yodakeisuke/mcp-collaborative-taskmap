export const toolDescription = `
As a senior developer, keep driving the implementation forward, ensuring every acceptance criterion is met—and provide granular progress updates along the way.
`;

export const nextAction = () => `
You MUST do the following step next:
    1. While building, keep asking yourself whether new insights call for tweaks to the implementation plan or acceptance criteria.
    2. If they do, run another refinement session.
    3. If not, forge ahead with the build—and when everything’s wrapped up, hand it off for review.
`